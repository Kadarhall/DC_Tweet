<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= image_tag current_user.profile_image %>
  <%= current_user.name %>
</p>

<p>
  <strong>Tweet:</strong>
  <%= @tweet.body %>
  <%= @tweets %>
</p>

<%= link_to 'Edit', edit_tweet_path(@tweet) %> |
<%= link_to 'Back', tweets_path %>

<div id='likes'>
	<%= render partial: 'likes', locals: {tweet: @tweet} %>
</div>